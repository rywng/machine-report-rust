[package]
name = "machine-report"
version = "0.1.0"
edition = "2024"
description = "Rust implementation of USGC's machine report"

[dependencies]
anyhow = "1.0.103"
chrono = { version = "0.4.45", features = ["alloc"] }
crossterm = "0.29.0"
rustix = { version = "1.1.4", features = ["system"] }
